package org.sully.d2.itemtracking;

import org.sully.d2.gamemodel.D2Item;
import org.sully.d2.gamemodel.enums.ItemQuality;

import java.util.function.Consumer;

public class MagicDamagePrefixes  {
	/*
	int savage;
	int merciless;
	int ferocious;
	int cruel;

	@Override
	public void consume(D2Item item, ItemNotifier notifier) {
		if (!(item.getQuality() == ItemQuality.MAGIC && item.getItemType().getWeaponInfo() != null)) {
			return;
		}
		if (item.getName().startsWith("Savage")) {
			savage++; 
		} else if (item.getName().startsWith("Merciless")) {
			merciless++;
		} else if (item.getName().startsWith("Ferocious")) {
			ferocious++;
		} else if (item.getName().startsWith("Cruel")) {
			cruel++;
		}
	}

	@Override
	public void closeAndGenerateOutput() {
		System.out.println("Magic Weapon Damage Prefixes -----------");
		System.out.println("Savage : " + savage);
		System.out.println("Merciless : " + merciless);
		System.out.println("Ferocious : " + ferocious);
		System.out.println("Cruel : " + cruel);
	}*/
	
}